body {
  background: black;
  color: white;
  text-align: center;
  font-family: Arial, sans-serif;
  overflow-x: hidden;
}

h1 {
  margin-top: 20px;
}

#reader {
  width: 300px;
  margin: 20px auto;
}

/* Flash effect when QR is scanned */
#overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: white;
  opacity: 0;
  pointer-events: none;
}

.flash {
  animation: flash 0.4s ease;
}

@keyframes flash {
  from { opacity: 1; }
  to { opacity: 0; }
}

/* Output area */
#output {
  display: none;
  margin-top: 30px;
}

/* Animated text */
#text {
  font-size: 50px;
  letter-spacing: 5px;
  animation: flyIn 1s ease, glow 1.5s infinite alternate;
}

@keyframes flyIn {
  from {
    transform: translateY(-100px) scale(0);
    opacity: 0;
  }
  to {
    transform: translateY(0) scale(1);
    opacity: 1;
  }
}

@keyframes glow {
  from {
    text-shadow: 0 0 10px cyan;
  }
  to {
    text-shadow: 0 0 40px magenta;
  }
}

/* Video animation */
video {
  width: 80%;
  margin-top: 20px;
  border-radius: 12px;
  animation: zoomFade 1.2s ease;
  box-shadow: 0 0 25px cyan;
}

@keyframes zoomFade {
  from {
    transform: scale(0.5);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}
